query notification($input: NotificationsInput!) {
  notifications(input: $input) {
    id
    type
    action
    actor_id
    action_target_id
    is_read
    created_at
  }
}

query notificationCount {
  notificationCount
}
