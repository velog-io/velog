import SettingEmailRow from './SettingEmailRow'
import { render } from '@testing-library/react'

describe('SettingEmailRow', () => {
  it('renders successfully', () => {
    render(<SettingEmailRow />)
  })
})
