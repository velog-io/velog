import VelogSearchPosts from './VelogSearchPosts'
import { render } from '@testing-library/react'

describe('VelogSearchPosts', () => {
  it('renders successfully', () => {
    render(<VelogSearchPosts />)
  })
})
