import VelogFollowings from './VelogFollowings'
import { render } from '@testing-library/react'

describe('VelogFollowings', () => {
  it('renders successfully', () => {
    render(<VelogFollowings />)
  })
})
