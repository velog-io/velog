import TrendingWriterGrid from './TrendingWriterGrid'
import { render } from '@testing-library/react'

describe('TrendingWriterGrid', () => {
  it('renders successfully', () => {
    render(<TrendingWriterGrid />)
  })
})
