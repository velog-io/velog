import HeaderUserMenuItem from './HeaderUserMenuItem'
import { render, screen } from '@testing-library/react'

describe('HeaderUserMenuItem', () => {
  it('renders successfully', () => {
    render(<HeaderUserMenuItem />)
  })
})
