import ChunkErrorScreen from './ChunkErrorScreen'
import { render } from '@testing-library/react'

describe('ChunkErrorScreen', () => {
  it('renders successfully', () => {
    render(<ChunkErrorScreen />)
  })
})
