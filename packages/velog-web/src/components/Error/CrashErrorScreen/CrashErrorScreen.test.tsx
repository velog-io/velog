import CrashErrorScreen from './CrashErrorScreen'
import { render } from '@testing-library/react'

describe('CrashErrorScreen', () => {
  it('renders successfully', () => {
    render(<CrashErrorScreen />)
  })
})
