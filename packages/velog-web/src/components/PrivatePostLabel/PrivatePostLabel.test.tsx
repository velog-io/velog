import PrivatePostLabel from './PrivatePostLabel'
import { render } from '@testing-library/react'

describe('PrivatePostLabel', () => {
  it('renders successfully', () => {
    render(<PrivatePostLabel />)
  })
})
