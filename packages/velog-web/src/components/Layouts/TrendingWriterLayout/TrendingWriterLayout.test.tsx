import TrendingWriterLayout from './TrendingWriterLayout'
import { render } from '@testing-library/react'

describe('TrendingWriterLayout', () => {
  it('renders successfully', () => {
    render(<TrendingWriterLayout />)
  })
})
