import HomeHeadExtra from './HomeHeadExtra'
import { render, screen } from '@testing-library/react'

describe('HomeHeadExtra', () => {
  it('renders successfully', () => {
    render(<HomeHeadExtra />)
  })
})
