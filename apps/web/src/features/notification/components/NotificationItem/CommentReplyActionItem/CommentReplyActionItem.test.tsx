import CommentReplyActionItem from './CommentReplyActionItem'
import { render } from '@testing-library/react'

describe('CommentReplyActionItem', () => {
  it('renders successfully', () => {
    render(<CommentReplyActionItem />)
  })
})
