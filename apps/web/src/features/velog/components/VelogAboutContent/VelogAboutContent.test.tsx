import VelogAboutContent from './VelogAboutContent'
import { render } from '@testing-library/react'

describe('VelogAboutContent', () => {
  it('renders successfully', () => {
    render(<VelogAboutContent />)
  })
})
