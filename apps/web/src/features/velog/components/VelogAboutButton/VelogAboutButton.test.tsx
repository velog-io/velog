import VelogAboutButton from './VelogAboutButton'
import { render } from '@testing-library/react'

describe('VelogAboutButton', () => {
  it('renders successfully', () => {
    render(<VelogAboutButton />)
  })
})
