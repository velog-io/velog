import PostFollowButton from './PostFollowButton'
import { render } from '@testing-library/react'

describe('PostFollowButton', () => {
  it('renders successfully', () => {
    render(<PostFollowButton />)
  })
})
