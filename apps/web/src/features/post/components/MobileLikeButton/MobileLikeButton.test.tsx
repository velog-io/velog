import MobileLikeButton from './MobileLikeButton'
import { render } from '@testing-library/react'

describe('MobileLikeButton', () => {
  it('renders successfully', () => {
    render(<MobileLikeButton />)
  })
})
