import HomeMoreButton from './HomeMoreButton'
import { render } from '@testing-library/react'

describe('HomeMoreButton', () => {
  it('renders successfully', () => {
    render(<HomeMoreButton />)
  })
})
