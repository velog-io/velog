import HeaderUserIcon from './HeaderUserIcon'
import { render } from '@testing-library/react'

describe('HeaderUserIcon', () => {
  it('renders successfully', () => {
    render(<HeaderUserIcon />)
  })
})
