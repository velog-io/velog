import FlatPostCardList from './FlatPostCardList'
import { render } from '@testing-library/react'

describe('FlatPostCardList', () => {
  it('renders successfully', () => {
    render(<FlatPostCardList />)
  })
})
