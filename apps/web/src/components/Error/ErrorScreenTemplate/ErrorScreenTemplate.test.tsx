import ErrorScreenTemplate from './ErrorScreenTemplate'
import { render } from '@testing-library/react'

describe('ErrorScreenTemplate', () => {
  it('renders successfully', () => {
    render(<ErrorScreenTemplate />)
  })
})
