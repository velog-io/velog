query userTags($input: UserTagsInput!) {
  userTags(input: $input) {
    tags {
      id
      name
      description
      posts_count
      thumbnail
    }
    posts_count
  }
}

query tag($name: String!) {
  tag(name: $name) {
    id
    name
    posts_count
    description
    thumbnail
  }
}
