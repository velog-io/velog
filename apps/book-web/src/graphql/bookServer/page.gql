query getPages($input: GetPagesInput!) {
  pages(input: $input) {
    id
    title
    url_slug
    type
    parent_id
    code
    childrens {
      id
      title
      url_slug
      type
      parent_id
      code
      childrens {
        id
        title
        url_slug
        type
        parent_id
        code
      }
    }
  }
}

mutation createPage($input: CreatePageInput!) {
  create(input: $input) {
    id
  }
}
