query getPageMetadata($input: GetPageMetadataInput!) {
  getPageMetadata(input: $input) {
    title
    url_slug
    type
    childrens {
      title
      url_slug
      type
      childrens {
        title
        url_slug
        type
      }
    }
  }
}
