import "../env";
import { mainVpc } from "./vpc";
import { getEcrRepository, getLatestImage } from "./ecs";

export = async () => {
  // const repositoryName = ENV.ecrRepositoryName;
  // const repository = await getEcrRepository(repositoryName);
  // const image = getLatestImage(repository);
  // return { repository };
};
